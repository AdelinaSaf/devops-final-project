apiVersion: v1
kind: Service
metadata:
  name: culinary-service
  namespace: adelina24052026
spec:
  selector:
    app: culinary-app
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    nodePort: 30080  # Фиксированный порт для доступа
  type: NodePort